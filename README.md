o	Backend:
	Ensure your ASP.NET Core API is running (e.g., via dotnet run) on https://localhost:7167/.
	Confirm CORS is configured to allow requests from your frontend (e.g., http://localhost:3000).
o	Frontend:
	Run your React application using npm start or yarn start.
	The app should open in your default browser at http://localhost:3000.
